#include<stdio.h>
main(){
     int n;
     printf("Enter a no.");
     scanf("%d",&n);
     (n & 1==1) ? printf("%d is an odd no.",n); printf("%d is an even",n);
     printf("\n");
     return 0;
}
}
